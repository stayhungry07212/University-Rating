import { FacilityIconsPipe } from './facility-icons.pipe';

describe('FacilityIconsPipe', () => {
  it('create an instance', () => {
    const pipe = new FacilityIconsPipe();
    expect(pipe).toBeTruthy();
  });
});
