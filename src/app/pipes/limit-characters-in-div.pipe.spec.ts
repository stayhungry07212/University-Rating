import { LimitCharactersInDivPipe } from './limit-characters-in-div.pipe';

describe('LimitCharactersInDivPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitCharactersInDivPipe();
    expect(pipe).toBeTruthy();
  });
});
