import { InstantFacilityTranslatePipe } from './instant-facility-translate.pipe';

describe('InstantFacilityTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new InstantFacilityTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
