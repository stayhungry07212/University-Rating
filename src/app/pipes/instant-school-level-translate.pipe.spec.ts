import { InstantSchoolLevelTranslatePipe } from './instant-school-level-translate.pipe';

describe('InstantSchoolLevelTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new InstantSchoolLevelTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
